# 当前文件职责：为 agent_ws 暴露集群内 WebSocket 访问入口。
apiVersion: v1
kind: Service
metadata:
  name: leary-agent
  labels:
    app: leary-agent
spec:
  selector:
    app: leary-agent
  ports:
    - name: ws
      port: 8081
      targetPort: ws
