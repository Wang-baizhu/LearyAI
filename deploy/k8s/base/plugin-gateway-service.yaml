# 当前文件职责：暴露模板插件独立预览网关的集群内访问入口。
apiVersion: v1
kind: Service
metadata:
  name: leary-plugin-gateway
spec:
  selector:
    app: leary-plugin-gateway
  ports:
    - name: http
      port: 7999
      targetPort: http
