#
# 当前文件职责：为 learyai 前端静态产物提供 Nginx 站点配置，并支持单页应用路由回退。
#
server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
