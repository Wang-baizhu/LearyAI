# 当前文件职责：为 test-external-kb 覆盖 backend 镜像标签与拉取策略。
apiVersion: apps/v1
kind: Deployment
metadata:
  name: leary-backend
spec:
  template:
    spec:
      containers:
        - name: leary-backend
          imagePullPolicy: IfNotPresent
