# 当前文件职责：为 tasks_server 暴露集群内 metrics 访问入口，供 Prometheus 抓取任务服务指标。
apiVersion: v1
kind: Service
metadata:
  name: leary-task
  labels:
    app: leary-task
spec:
  selector:
    app: leary-task
  ports:
    - name: metrics
      port: 8023
      targetPort: metrics
