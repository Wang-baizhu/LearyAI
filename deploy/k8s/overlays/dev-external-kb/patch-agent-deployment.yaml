# 当前文件职责：为 dev-external-kb 覆盖 agent 镜像标签与拉取策略。
apiVersion: apps/v1
kind: Deployment
metadata:
  name: leary-agent
spec:
  template:
    spec:
      containers:
        - name: leary-agent
          image: leary-agent:dev
          imagePullPolicy: IfNotPresent

