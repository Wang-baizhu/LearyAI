# Agent说明（review-task 模块）

## 模块目标

承载管理端资源发布人工审核任务列表与审批通过动作。

## 协作约束

- 只调用 `/api/admin/review-tasks` 相关管理员接口。
- 本期只展示人工待审任务并支持“审批通过”，不在前端引入驳回、批注或自动检查流程。
- API 契约通过 `src/shared/types/api.ts` 复用后端类型别名，禁止在组件中散落接口字段定义。
