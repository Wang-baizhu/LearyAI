# 当前文件职责：为集群内 Redis 提供稳定访问入口与 StatefulSet 网络标识。
apiVersion: v1
kind: Service
metadata:
  name: leary-redis
  labels:
    app: leary-redis
spec:
  clusterIP: None
  selector:
    app: leary-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
